<section class="container">
  <h3 class="potluck-name Hour:" {{format_time potluck.startDateTime}}</h4>
  <h4>Closing Hour: {{format_time potluck.endDateTime}}</h4>
  <section class="flex-row justify-center">
    {{#each potluck.food as |food|}}
    {{> food-details}}
    {{/each}}
  </section>
</section>
