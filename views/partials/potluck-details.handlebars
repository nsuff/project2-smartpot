<section class="card">
  <a href="/potluck/{{id}}">
    <header>{{name}}</header>
    <p>{{description}}</p>
    <!--<img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}">-->
  </a>
</section>
