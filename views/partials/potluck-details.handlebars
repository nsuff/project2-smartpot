{{!-- <div class="card">
  <div class="card-content">
    <a href="/potluck/{{id}}">
      <header class="title">
        {{ name }}
      </header>
    </a>
    <p class="subtitle">
      {{description}}
    </p>
  </div>
</div> --}}

  <div class="posts">
    <div class="post">
      <div class="section">
        <h1> {{ name }} </h1> 
        <p>{{description}}</p>
        <p3>{{startDate}}</p3>
        <a href="/potluck/{{id}}" class="readmore">Details</a>
      </div>
    </div>
  </div>
