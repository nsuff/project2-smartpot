<section class="card centerall">
  <header>{{this.user.username}}</header>
  <header>{{this.name}}</header>

  <div class="card-body">
    <p>{{this.description}}</p>
  </div>

  <a href="/editfood/{{id}}"><h3 class="edit-food-btn">Edit</h3></a>

</section>
